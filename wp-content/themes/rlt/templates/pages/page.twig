{% extends "base.twig" %}

{% block content %}

<div id="{{ post.title }}">
	<div class="page-header bg-breeze-blue">
		<h1>{{ post.title }}</h1>
	</div>
	<article class="dynamic-content">
		{{ post.content }}
	</article>
</div>

{% endblock %}